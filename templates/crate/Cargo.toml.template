[package]
name = "{{name}}"
version = "0.1.0"
edition = "2021"
description = "Axiom {{name}} crate"
license = "MIT"
repository = "https://github.com/arwei944/axiom-kernel"

[lib]
name = "{{lib_name}}"
path = "src/lib.rs"

[dependencies]
{{dependencies}}

[dev-dependencies]
tokio = {{ version = "1.0", features = ["full"] }}
