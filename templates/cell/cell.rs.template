//! {{name}} - {{layer}} layer Cell
//!
//! # Architecture Constraints
//!
//! - This Cell can only send signals to: {{allowed_targets}}

use axiom_kernel::cell::{Cell, CellId, CellKind};
use axiom_kernel::id::CellId as KernelCellId;

#[derive(Debug, Default)]
pub struct {{name}};

impl Cell for {{name}} {
    fn cell_id(&self) -> KernelCellId {
        KernelCellId::new("{{snake_name}}")
    }

    fn cell_kind(&self) -> CellKind {
        {{cell_kind}}
    }
}